events {}

http {

    
    upstream backend_servers {
    server app:3000 max_fails=3 fail_timeout=30s;
     }



    server {
        listen 80;

        location / {
            proxy_pass http://backend_servers;
        }
    }
}