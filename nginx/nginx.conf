events {}

http {

   upstream backend_servers {
    server app:3000;
    server app:3001;
    server app:3002;
}

    server {
        listen 80;

        location / {
            proxy_pass http://backend_servers;
        }
    }
}